export default function Home() {
  return (
    <main className="min-h-dvh grid place-items-center p-6">
      <section className="w-full max-w-md rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6 shadow-sm">
        <p className="text-sm font-semibold text-[var(--primary)]">DOCENCIA360</p>
        <h1 className="mt-2 text-2xl font-bold tracking-tight">La base está lista.</h1>
        <p className="mt-2 text-sm text-[var(--muted)]">
          La interfaz de autenticación y onboarding será el siguiente bloque.
        </p>
      </section>
    </main>
  )
}
